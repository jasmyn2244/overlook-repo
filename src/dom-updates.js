let domUpdates = {

  hide(elements) {
    forEach(element => {
      element.classList.add('hidden')
    })
  },

  show(elements) {
    forEach(element) {
      element.classList.remove('hidden')
    }
  },



}
